import ChatInterface from '@/components/features/ChatInterface';

const Chat = () => {
  return (
    <div className="h-screen flex flex-col">
      <ChatInterface />
    </div>
  );
};

export default Chat;
